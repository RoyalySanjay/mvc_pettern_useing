 import 'package:flutter/material.dart';

import '../../colors.dart';

var lightmode=ThemeData(
  scaffoldBackgroundColor:normalTextColor,
  appBarTheme: AppBarTheme(
    titleTextStyle:TextStyle(
      color: Colors.black,

    ),
  ),
);